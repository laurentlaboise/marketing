# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# ===================================
# Firebase Configuration
# ===================================
# Get these from: Firebase Console > Project Settings > General > Your apps > SDK setup and configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef123456
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ===================================
# Sentry Configuration (Error Tracking)
# ===================================
# Get your DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
VITE_SENTRY_DSN=https://your_sentry_dsn@sentry.io/1234567

# Sentry Environment
VITE_SENTRY_ENVIRONMENT=production

# ===================================
# Build Configuration
# ===================================
# Set to 'production' or 'development'
NODE_ENV=production
VITE_MODE=production

# ===================================
# Feature Flags
# ===================================
# Enable/disable features
VITE_ENABLE_SENTRY=true
VITE_ENABLE_WEB_VITALS=true
VITE_ENABLE_CIRCUIT_BREAKER=true

# ===================================
# Security
# ===================================
# reCAPTCHA Site Key (v3)
# Get from: https://www.google.com/recaptcha/admin
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key

# IMPORTANT SECURITY NOTES:
# ========================
#
# 1. Firebase API Keys:
#    - Firebase API keys ARE safe to expose in client-side code
#    - Security is enforced by Firestore Security Rules (see firestore.rules)
#    - However, using environment variables is still a best practice
#
# 2. Sentry DSN:
#    - Sentry DSN is also safe to expose (it's public-facing)
#    - It only allows sending errors, not reading them
#
# 3. DO NOT store:
#    - Database passwords
#    - Private API keys
#    - OAuth client secrets
#    - Admin credentials
#
# 4. For server-side secrets, use:
#    - Firebase Cloud Functions with Secret Manager
#    - GitHub Actions secrets for CI/CD
#    - Netlify/Vercel environment variables
#
# 5. Always add .env.local to .gitignore

# ===================================
# CI/CD Configuration
# ===================================
# These should be set in your CI/CD platform (GitHub Actions, etc.)
# FIREBASE_TOKEN=your_ci_token_here
# GITHUB_TOKEN=your_github_token_here
