[phases.setup]
nixPkgs = ['nodejs_20', 'bash']

[phases.install]
cmds = ['cd blog-backend && npm ci --prefer-offline --no-audit || npm install']

[phases.build]
cmds = ['chmod +x start-server.sh']

[start]
cmd = 'bash start-server.sh'
