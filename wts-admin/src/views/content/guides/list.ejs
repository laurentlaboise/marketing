<%- include('../../partials/head', { title, bodyClass: 'dashboard-layout' }) %>

<%- include('../../partials/sidebar', { currentPage, user }) %>

<main class="main-content">
  <%- include('../../partials/header', { user, messages }) %>

  <div class="page-content">
    <div class="page-header">
      <div>
        <h1 class="page-title">E-Guides</h1>
        <p class="page-subtitle">Manage your downloadable guides and resources</p>
      </div>
      <div class="page-actions">
        <a href="/content/guides/new" class="btn btn-primary">
          <i class="fas fa-plus"></i> New E-Guide
        </a>
      </div>
    </div>

    <div class="card">
      <div class="card-header">
        <form action="/content/guides" method="GET" style="display: flex; gap: 12px; flex-wrap: wrap;">
          <input type="text" name="search" class="form-input" placeholder="Search guides..." value="<%= pagination.search || '' %>" style="width: 200px;">
          <select name="status" class="form-input" style="width: 150px;">
            <option value="">All Status</option>
            <option value="draft" <%= pagination.status === 'draft' ? 'selected' : '' %>>Draft</option>
            <option value="published" <%= pagination.status === 'published' ? 'selected' : '' %>>Published</option>
          </select>
          <button type="submit" class="btn btn-secondary btn-sm">
            <i class="fas fa-search"></i> Search
          </button>
        </form>
      </div>
      <div class="card-body" style="padding: 0;">
        <% if (guides.length > 0) { %>
        <div class="table-container">
          <table class="table">
            <thead>
              <tr>
                <th>Title</th>
                <th>Category</th>
                <th>Status</th>
                <th>Created</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% guides.forEach(guide => { %>
              <tr>
                <td>
                  <strong><%= guide.title %></strong>
                  <br>
                  <small style="color: var(--gray-500);">/<%= guide.slug %></small>
                </td>
                <td><%= guide.category || '-' %></td>
                <td>
                  <span class="status-badge <%= guide.status %>"><%= guide.status %></span>
                </td>
                <td><%= new Date(guide.created_at).toLocaleDateString() %></td>
                <td>
                  <div class="table-actions">
                    <a href="/content/guides/<%= guide.id %>/edit" class="btn btn-sm btn-secondary" title="Edit">
                      <i class="fas fa-edit"></i>
                    </a>
                    <form action="/content/guides/<%= guide.id %>/delete" method="POST" style="display: inline;">
                      <button type="submit" class="btn btn-sm btn-danger" title="Delete" data-confirm-delete="Are you sure you want to delete this guide?">
                        <i class="fas fa-trash"></i>
                      </button>
                    </form>
                  </div>
                </td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </div>

        <% if (pagination.totalPages > 1) { %>
        <div class="card-footer">
          <div class="pagination">
            <% if (pagination.page > 1) { %>
            <a href="?page=<%= pagination.page - 1 %>&search=<%= pagination.search || '' %>&status=<%= pagination.status || '' %>" class="pagination-btn">
              <i class="fas fa-chevron-left"></i>
            </a>
            <% } %>

            <% for (let i = 1; i <= pagination.totalPages; i++) { %>
            <a href="?page=<%= i %>&search=<%= pagination.search || '' %>&status=<%= pagination.status || '' %>" class="pagination-btn <%= i === pagination.page ? 'active' : '' %>">
              <%= i %>
            </a>
            <% } %>

            <% if (pagination.page < pagination.totalPages) { %>
            <a href="?page=<%= pagination.page + 1 %>&search=<%= pagination.search || '' %>&status=<%= pagination.status || '' %>" class="pagination-btn">
              <i class="fas fa-chevron-right"></i>
            </a>
            <% } %>
          </div>
        </div>
        <% } %>
        <% } else { %>
        <div class="empty-state">
          <div class="empty-state-icon">
            <i class="fas fa-book-reader"></i>
          </div>
          <h3>No E-Guides Found</h3>
          <p>Get started by creating your first e-guide.</p>
          <a href="/content/guides/new" class="btn btn-primary">
            <i class="fas fa-plus"></i> Create E-Guide
          </a>
        </div>
        <% } %>
      </div>
    </div>
  </div>
</main>

<%- include('../../partials/footer') %>
